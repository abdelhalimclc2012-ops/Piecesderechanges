- name: Build APK
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 60
          max_attempts: 4
          retry_wait_seconds: 30
          command: yes | buildozer android debug
